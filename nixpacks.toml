[providers]
php = { php = "^8.4", composer = true }

[phases.setup]
cmds = ["composer install --no-interaction"]

[phases.build]
cmds = ["php artisan build"]

[start]
cmd = "php artisan serve --host 0.0.0.0 --port $PORT"

